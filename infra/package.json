{
  "name": "@macromap/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap:synth": "cdk synth --quiet --app 'npx tsx bin/bootstrap.ts'",
    "diff": "cdk diff --change-set=false",
    "synth": "cdk synth --quiet",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.265.0",
    "constructs": "^10.8.1"
  }
}
